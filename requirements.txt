# Core Flask dependencies
Flask==3.0.0
flask-cors==4.0.0
flask-limiter==3.5.0
python-dotenv==1.0.0

# Server
gunicorn==21.2.0

# AI Providers
openai==1.98.0
anthropic==0.8.1

# Data handling
Jinja2==3.1.2
requests==2.31.0

# Redis for caching (optional, but recommended for production)
redis==5.0.1

# File processing for pitch deck builder
PyPDF2==3.0.1          # PDF text extraction
python-docx==1.1.0     # Microsoft Word document processing
python-pptx==0.6.23    # PowerPoint generation
Pillow==10.2.0         # Image processing for meme cards

# PDF generation (optional - has system dependencies)
# Uncomment if you have the system libraries installed
# weasyprint==60.2

# Additional utilities
python-dateutil==2.8.2